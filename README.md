# largest
//Largest of three numbers
#include <iostream>
using namespace std;

int main() {    
    float a, b, c,largest;

     //   try this
        largest=a>b?(a>c?a:c):(b>c?b:c);
        cout << "Largest number: " << largest;
    return 0;
}
